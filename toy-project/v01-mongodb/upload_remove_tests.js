const {URI} = require('./_config.js');
const removeAll = require('../util/remove_all.js');
const uploadJSON = require('./util/upload_json.js');
const util = require('../util/mongodbutil0.js')

(async () => {
    try{
        

    }catch(error) {
        console.error(error);
    }

})();
